# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

pod 'Alamofire'
pod 'SwiftyBeaver'

target 'WebApiTests' do
    pod 'PKHUD' #b/c AppDelegate import otherwise we should NOT use it here!
end

target 'WebApi' do
    pod 'PKHUD'
    pod 'Eureka'
end
